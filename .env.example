# Docker Compose configuration (copy to .env and adjust as needed)

# Public port for the web app (default 14001)
APP_PORT=14001

# Public port for MySQL (default 14002)
DB_PORT=14002

# Public URL guests will use (must match APP_PORT unless behind a proxy)
ORIGIN=http://localhost:14001